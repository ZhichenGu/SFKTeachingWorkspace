# SFK Teaching Workspace

- 工作目录以本仓库为准。只处理学生上课签单及已授权的管理、签名、导出流程。
- 用户已授权范围内的可逆调整直接实施；不把计划、预览、字段缺项当作新的批准关卡。
- 保持静态 GitHub Pages 前端。共享持久化用已有 Supabase，不引入另一套后端、框架或登录系统。
- 保留数据与界面职责边界。`renderDocument(record)` 是预览、打印、单份和批量图片的统一版式来源。
- 云端只存文字和活动签名，每张签名图片最多 16 KB；完整 A4 图片在客户端生成。不要静默压糊、裁切或丢失签名。
- `polishService.provider` 只作为扩展入口。不要硬编码模型名或添加未经请求的 AI API 调用。
- RLS 和受限 RPC 是权限边界；不能将 service_role / secret key 放入前端。不能仅依靠禁用按钮保护老师字段。
- 记录版本冲突不能用覆盖重试解决；不确定的学生提交先查询实际签名状态。
- 非关键缺项允许预览与发送；学生未签名正是发链接的正常状态。删除只需一次明确确认。
- 验证与变更相称：关注手机溢出、签名状态、导出完整性、双端同步和权限。模拟接口测试不能冒充真实数据库验证。
- 不自动提交、推送、发布或执行外部数据库变更，除非任务已授权这些动作。缺少真实环境配置时完成所有不依赖它的工作，并准确说明最小剩余步骤。
